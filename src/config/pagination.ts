/**
 * Размер страницы списков — постов и рецептов одинаково.
 *
 * Лежит в `config`, а не в `helpers`: это настройка приложения, а не утилита.
 * Номер текущей страницы живёт в URL, см. `@/helpers/usePageParam`.
 */
export const PER_PAGE = 5;
