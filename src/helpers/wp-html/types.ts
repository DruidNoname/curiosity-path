/**
 * Шаг конвейера обработки HTML из WordPress.
 *
 * Каждый шаг мутирует переданный документ на месте и ничего не возвращает.
 * Порядок шагов задан в `transforms/index.ts` и имеет значение: спойлеры
 * разворачиваются до чистки пустых параграфов, картинки оборачиваются после неё.
 */
export type HtmlTransform = (doc: Document) => void;
